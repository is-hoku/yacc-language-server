(menhir
 (modules syntax))

(library
 (name static_analyzer)
 (public_name language_server.static_analyzer)
 (libraries language_server))

(rule
 (target lexer.ml)
 (deps   lexer.mll)
 (action (chdir %{workspace_root}
          (run %{bin:ocamllex} -ml -q -o %{target} %{deps}))))
